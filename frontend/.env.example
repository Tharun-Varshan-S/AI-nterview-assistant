# API Base URL - Local development
VITE_API_BASE_URL=http://localhost:5000/api

# Note: Judge0 code execution now uses the backend proxy
# RapidAPI configuration is no longer needed
# The backend connects directly to local Judge0 at http://localhost:2358
